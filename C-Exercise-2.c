#include<stdio.h>

//Pozitif tamsayı girildiği zaman çözüm programı


/*
int main ()
    {
        int sayi,kalan;

        printf("Bir sayı giriniz:");
        scanf("%d",&sayi);

        kalan=sayi%2;

        if (kalan==1)
        printf("Girilen sayı tektir");
        else
        printf("Girilen sayı çifttir");

        getchar();
        getchar();

    }
    */

   //Girilen üç tam sayıdan en büyüğünü bulan program

/*
   int main()
   {
       int sayi1,sayi2,sayi3;
       printf ("3 Sayı giriniz:");
       scanf("%d%d%d",&sayi1,&sayi2,&sayi3);

       if 
       (sayi1>=sayi2 && sayi1>=sayi3)
       printf ("En büyük sayı = %d", sayi2);
       else
       printf ("En büyük sayı = %d", sayi3);

       getchar();
       getchar();
   }

   */
//1OO Üzerinden geçme ve kalma durumu hesaplayan program

/*
  int main()
  {
      int puan;
      printf("0-100 Arası bir sayı giriniz:");
      scanf("%d", &puan);
      if (puan>=0 && puan<=44)
      printf("Başarısız");
      if (puan>=45 && puan<=54)
      printf("Geçer");
      if (puan>=55 && puan<=69)
      printf("Orta");
      if (puan >=70 && puan<=84)
      printf("İyi");
      if (puan >=85 && puan<=100)
      printf("Pekiyi");

      getchar();
      getchar();
  }
  */